// java program to find power of a number using functions

import java.util.Scanner;
public class powernumberfunctions {
    public static void my_function(int base,int exponent){
        int power=(int)Math.pow(base, exponent);
        System.out.println("Power of a number = "+power);
    }
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        System.out.println("Enter the base value:");
        int base=sc.nextInt();
        System.out.println("Enter the exponenet value:");
        int exponent=sc.nextInt();
        my_function(base, exponent);
        sc.close();
    }
}

output:
Enter the base value:
4
Enter the exponenet value:
2
Power of a number = 16
